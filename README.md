# Goku - JSON <-> YAML Converter

A simple CLI project written in Go that converts:

- JSON to YAML
- YAML to JSON

## Features

- Convert `.json` to `.yaml`
- Convert `.yaml` to `.json`
- Input and output file support using flags
- Simple CLI interface

